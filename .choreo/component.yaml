schemaVersion: 1.0
endpoints:
  - name: chat_websocket
    service:
      port: 8080    # Port exposed to Choreo
      targetPort: 8080  # Ensure your WebSocket server is listening on this port
    type: WS
    schemaFilePath: asyncapi.yaml
